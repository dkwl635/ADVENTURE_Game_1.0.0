# 시스템 아키텍처 리팩토링: 강결합 구조 해소 및 모듈화

## 1. 리팩토링 배경 및 목적 (AS-IS)

기존 프로젝트는 초기 빠른 프로토타이핑과 기능 개발을 위해 대부분의 매니저 클래스들이 **싱글톤(Singleton) 패턴** 기반으로 구현되어 있었습니다. (`ex: CoreMgr, ItemMgr, SoundMgr 등`) 
프로젝트의 규모가 커지고 다양한 시스템이 추가됨에 따라 아래와 같은 구조적 문제점(기술 부채)이 발생하였고, 이를 해결하여 프로젝트의 유지보수성과 확장성을 높이고자 리팩토링을 진행합니다.

### 🔴 현재 구조의 주요 문제점
1.  **전역 접근 및 강한 결합(High Coupling):**
    *   모든 클래스에서 `Manager.Inst.DoSomething()` 형태로 스크립트 내부 깊숙한 곳에서 다른 매니저를 호출함.
    *   클래스 파일 겉부분(헤더나 생성자)만 봐서는 해당 클래스가 어떤 매니저에 의존하고 있는지 전혀 파악할 수 없는 **숨겨진 의존성(Hidden Dependency)** 발생.
2.  **데이터 계층과 View(UI) 계층의 혼재:**
    *   `ItemMgr` 같은 데이터 제어 클래스가 다른 오브젝트 생성(`Instantiate`)이나 UI 조작(`SpanwDamageTxt`) 등에 직접 관여하며 핵심 비즈니스 로직과 화면의 렌더링 로직이 섞여 단일 책임 원칙(SRP)을 위배함.
3.  **순서 제어 불가 및 확장 제약:**
    *   각 싱글톤이 각자의 `Awake()` 와 `DontDestroyOnLoad`를 통해 동시다발적으로 생성되므로, 초기화 순서(Initialization Order)를 보장할 수 없음.
    *   단위 테스트나 특정 모듈만 떼어내어 테스트하기 위해선 거대한 싱글톤 객체들이 전부 씬에 필요해지는 기형적 구조.

---

## 2. 리팩토링 전략 및 해결 방안 (TO-BE)

위 문제점들을 해결하기 위해 다음과 같이 3단계 관점으로 아키텍처를 개선합니다.

### 🟢 1) Service Locator 패턴 적용
싱글톤을 대체하기 위해 서비스 관리 중앙 통제 센터인 `ServiceLocator` 개념을 도입합니다. 모든 매니저 객체에 직접 접근하는 것을 차단하고, 각 매니저는 이 로케이터에 자신의 기능을 등록(Register)하여 중앙 통제 방식으로 관리됩니다.

> **적용 전:** `SoundMgr.Inst.Play("Click");`
> **적용 후:** `ServiceLocator.Get<IAudioService>().Play("Click");`

### 🟢 2) 인터페이스(Interface) 기반 역할 분리
각 매니저 클래스가 외부에 노출할 기능만을 `IInventoryService`, `IAudioService`, `IDataService` 등 인터페이스로 추상화하여 제공합니다. 
이로 인해 객체 간의 결합이 "구체적인 클래스"가 아닌 "기능 명세(추상화)"에 의존하게 되어(DIP 원칙 준수), 나중에 핵심 로직 시스템을 수정하거나 교체할 때 타 모듈(클라이언트 코드)을 수정할 필요가 대폭 줄어듭니다.

### 🟢 3) MVC/MVP 패턴에 입각한 이벤트 기반 시스템 (Event-Driven)
매니저가 플레이어나 다른 UI의 함수를 직접 찾아가 호출하는 방식에서 벗어납니다. 대신, **매니저는 데이터가 변경되었음을 알리는 이벤트(`System.Action`, `delegate`)만 발생시키고 책임 모델을 끝내는 단방향(One-way) 흐름**으로 변경합니다.
UI(View)나 플레이어 오브젝트는 이벤트를 구독하고 대기하다가, 이벤트 알림이 오면 스스로 화면을 그리거나 상태를 변경하여 독립성을 확보합니다.

---

## 4. 기대 효과 (결론)

1.  **독립적인 모듈화 (Robustness):** A 모듈에서 에러나 수정이 발생해도, B 모듈의 코드 흐름에 연쇄적인 영향을 미치지 않는 견고한 결합 단절 구조 완성.
2.  **분업의 효율화 (Productivity):** 시각 효과 구현 파트(UI/연출)와 백그라운드 데이터 처리 파트(Manager)가 스크립트 단에서 완벽하게 분리되므로 병렬 작업 및 유지보수성 극대화.
3.  **수명 주기 통제 (Lifecycle Management):** 프로젝트 전체 수명 주기를 갖는 `Global Service`와 개별 씬 내에서만 생존하는 `Local Service`를 명확히 구분하여 리소스 관리와 불필요한 메모리 점유 방지.
